# Baileys WhatsApp Listener Environment Variables

# Backend API URL (where to send incoming messages)
BACKEND_URL=http://localhost:8080

# Security: Shared secret with backend
BAILEYS_SECRET=replace_with_secure_secret

# Session directory for Baileys auth state (QR code, credentials)
SESSION_DIR=./auth
